package stories.model.user.authenticator;

/**
 * Type of authentication. It is not classic Basic or Bearer. It is authentication that used by specific backend type.
 */
public enum AuthenticationType {
    AUTHENTICATION_TYPE_YOUR_PROJECT
}
